# AI News 技术架构

## 📊 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              前端 (Browser)                              │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    static/index.html                            │   │
│  │  • HTML5 + CSS3 + JavaScript (原生)                             │   │
│  │  • Fetch API 异步请求                                           │   │
│  │  • 响应式布局                                                    │   │
│  └──────────────────────────┬──────────────────────────────────────┘   │
└─────────────────────────────┼───────────────────────────────────────────┘
                              │ HTTP/REST API
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           后端 (Python)                                  │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                      FastAPI 应用层                              │   │
│  │  src/main.py - 应用入口                                          │   │
│  │  • CORS 中间件                                                   │   │
│  │  • 静态文件服务                                                   │   │
│  │  • API 路由注册                                                  │   │
│  └──────────────────────────┬──────────────────────────────────────┘   │
│                             │                                           │
│  ┌──────────────────────────┼──────────────────────────────────────┐   │
│  │                      API 路由层                                  │   │
│  │  src/api/routes/                                                 │   │
│  │  ├── news.py    - 新闻列表、详情、分类、来源                      │   │
│  │  ├── stats.py   - 统计数据                                       │   │
│  │  └── admin.py   - 管理接口（手动抓取）                            │   │
│  └──────────────────────────┬──────────────────────────────────────┘   │
│                             │                                           │
│  ┌──────────────────────────┼──────────────────────────────────────┐   │
│  │                      业务逻辑层                                  │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │   │
│  │  │  爬虫模块   │  │  处理模块   │  │      调度模块           │  │   │
│  │  │ src/scrapers│  │src/processors│  │   src/scheduler        │  │   │
│  │  │             │  │             │  │                         │  │   │
│  │  │• RSSScraper │  │• Deduplicator│  │• APScheduler           │  │   │
│  │  │• WebScraper │  │• Classifier │  │• BackgroundTasks       │  │   │
│  │  │• feedparser │  │• Summarizer │  │• CronTrigger           │  │   │
│  │  │             │  │• Scorer     │  │                         │  │   │
│  │  └─────────────┘  └─────────────┘  └─────────────────────────┘  │   │
│  └──────────────────────────┬──────────────────────────────────────┘   │
│                             │                                           │
│  ┌──────────────────────────┼──────────────────────────────────────┐   │
│  │                      数据访问层                                  │   │
│  │  src/database/connection.py                                      │   │
│  │  • SQLAlchemy ORM                                                │   │
│  │  • Session 管理                                                  │   │
│  │  • 连接池                                                        │   │
│  └──────────────────────────┬──────────────────────────────────────┘   │
└─────────────────────────────┼───────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          数据存储层                                      │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                      SQLite 数据库                               │   │
│  │  ainews.db                                                       │   │
│  │  • news 表 - 新闻数据                                            │   │
│  │  • news_source 表 - 来源                                         │   │
│  │  • news_category 表 - 分类                                       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 技术栈详解

### 1. 前端技术

| 技术 | 用途 | 文件 |
|------|------|------|
| **HTML5** | 页面结构 | `static/index.html` |
| **CSS3** | 样式、动画、响应式布局 | 内联在 HTML 中 |
| **JavaScript (ES6+)** | 交互逻辑 | 内联在 HTML 中 |
| **Fetch API** | 异步 HTTP 请求 | 调用后端 REST API |

**前端关键代码片段：**
```javascript
// 异步获取新闻列表
async function loadNews(page = 1) {
    const response = await fetch(`/api/news/?page=${page}`);
    const news = await response.json();
    // 渲染新闻卡片...
}

// 轮询抓取状态
setInterval(async () => {
    const status = await fetch('/api/admin/scrape/status');
    // 更新 UI...
}, 2000);
```

---

### 2. 后端技术

#### 2.1 Web 框架：FastAPI

| 组件 | 用途 |
|------|------|
| **FastAPI** | 高性能异步 Web 框架 |
| **Uvicorn** | ASGI 服务器 |
| **Pydantic** | 数据验证和序列化 |
| **Starlette** | 底层 ASGI 工具包 |

**API 端点：**
```
GET  /api/news/              # 新闻列表（支持分页、筛选）
GET  /api/news/{id}          # 新闻详情
GET  /api/news/categories/list  # 分类列表
GET  /api/news/sources/list     # 来源列表
GET  /api/stats/overview     # 统计概览
POST /api/admin/scrape/trigger  # 触发抓取
GET  /api/admin/scrape/status   # 抓取状态
```

#### 2.2 数据库：SQLAlchemy + SQLite

| 组件 | 用途 |
|------|------|
| **SQLAlchemy** | ORM 框架 |
| **SQLite** | 轻量级数据库 |

**数据模型：**
```python
# src/models/news.py
class News(Base):
    id: int              # 主键
    title: str           # 标题
    content: str         # 内容
    summary: str         # 摘要
    url: str             # 原文链接（唯一）
    source_id: int       # 来源 FK
    category_id: int     # 分类 FK
    importance_score: float  # 重要性评分
    is_featured: bool    # 是否精选
    published_at: datetime   # 发布时间
    scraped_at: datetime     # 抓取时间
```

#### 2.3 爬虫模块

| 技术 | 用途 |
|------|------|
| **feedparser** | RSS/Atom 解析 |
| **BeautifulSoup4** | HTML 解析 |
| **requests** | HTTP 请求 |
| **lxml** | 高性能 XML/HTML 解析 |

**RSS 爬虫流程：**
```
feedparser.parse(url)  →  解析 RSS 条目  →  提取字段  →  返回文章列表
```

#### 2.4 内容处理模块

| 模块 | 功能 | 算法 |
|------|------|------|
| **Deduplicator** | 去重 | URL 去重 + 标题相似度 |
| **Classifier** | 分类 | 关键词匹配 / AI 分类 |
| **Summarizer** | 摘要 | 截取首段 / AI 生成 |
| **Scorer** | 评分 | 加权评分算法 |

**评分算法：**
```python
score = (
    source_score * 0.2 +      # 来源权威性
    keyword_score * 0.3 +     # 关键词匹配
    length_score * 0.1 +      # 内容长度
    recency_score * 0.2 +     # 时效性
    engagement_score * 0.2    # 互动度
)
```

#### 2.5 定时任务

| 技术 | 用途 |
|------|------|
| **APScheduler** | Python 定时任务框架 |
| **CronTrigger** | Cron 表达式调度 |
| **BackgroundTasks** | FastAPI 后台任务 |

**调度配置：**
```yaml
# config.yaml
scheduler:
  daily_scrape_time: "09:00"  # 每天 9:00 执行
  timezone: "Asia/Shanghai"
  enabled: true
```

---

## 🔄 数据流转图

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   RSS 源     │────▶│  RSS Scraper │────▶│   原始数据   │
│ (外部网站)   │     │  (feedparser)│     │  (文章列表)  │
└──────────────┘     └──────────────┘     └──────┬───────┘
                                                 │
                                                 ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   去重结果   │◀────│  Deduplicator│◀────│   原始数据   │
│              │     │  (URL+标题)  │     │              │
└──────┬───────┘     └──────────────┘     └──────────────┘
       │
       ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   分类结果   │◀────│  Classifier  │◀────│   去重数据   │
│              │     │ (关键词匹配) │     │              │
└──────┬───────┘     └──────────────┘     └──────────────┘
       │
       ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   摘要结果   │◀────│  Summarizer  │◀────│   分类数据   │
│              │     │ (截取/AI)    │     │              │
└──────┬───────┘     └──────────────┘     └──────────────┘
       │
       ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   评分结果   │◀────│   Scorer     │◀────│   摘要数据   │
│              │     │ (加权算法)   │     │              │
└──────┬───────┘     └──────────────┘     └──────────────┘
       │
       ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   SQLite     │◀────│  SQLAlchemy  │◀────│   最终数据   │
│  (持久化)    │     │   (ORM)      │     │              │
└──────────────┘     └──────────────┘     └──────────────┘
```

---

## 🔌 前后端交互流程

### 1. 页面加载流程

```
┌─────────┐                    ┌─────────┐                    ┌─────────┐
│ Browser │                    │ FastAPI │                    │ SQLite  │
└────┬────┘                    └────┬────┘                    └────┬────┘
     │                              │                              │
     │  GET /static/index.html      │                              │
     │─────────────────────────────▶│                              │
     │                              │                              │
     │  HTML + CSS + JS             │                              │
     │◀─────────────────────────────│                              │
     │                              │                              │
     │  GET /api/news/categories/list                              │
     │─────────────────────────────▶│                              │
     │                              │  SELECT * FROM news_category │
     │                              │─────────────────────────────▶│
     │                              │             categories       │
     │                              │◀─────────────────────────────│
     │           JSON               │                              │
     │◀─────────────────────────────│                              │
     │                              │                              │
     │  GET /api/news/?page=1       │                              │
     │─────────────────────────────▶│                              │
     │                              │  SELECT * FROM news LIMIT 20 │
     │                              │─────────────────────────────▶│
     │                              │              news            │
     │                              │◀─────────────────────────────│
     │           JSON               │                              │
     │◀─────────────────────────────│                              │
     │                              │                              │
     │  渲染新闻列表                │                              │
     │                              │                              │
```

### 2. 手动抓取流程

```
┌─────────┐          ┌─────────┐          ┌─────────┐          ┌─────────┐
│ Browser │          │ FastAPI │          │ Scraper │          │ SQLite  │
└────┬────┘          └────┬────┘          └────┬────┘          └────┬────┘
     │                    │                    │                    │
     │ POST /api/admin/scrape/trigger          │                    │
     │───────────────────▶│                    │                    │
     │                    │                    │                    │
     │ {"success": true}  │  启动后台任务      │                    │
     │◀───────────────────│───────────────────▶│                    │
     │                    │                    │                    │
     │                    │                    │ 抓取 RSS           │
     │                    │                    │────────────────────│
     │                    │                    │                    │
     │ 轮询状态           │                    │ 处理 & 存储        │
     │ GET /scrape/status │                    │───────────────────▶│
     │───────────────────▶│                    │                    │
     │                    │                    │                    │
     │ {"status":"running"}                    │                    │
     │◀───────────────────│                    │                    │
     │                    │                    │                    │
     │  ...继续轮询...    │                    │ 完成               │
     │───────────────────▶│◀───────────────────│                    │
     │                    │                    │                    │
     │ {"status":"completed", "saved_count":80}│                    │
     │◀───────────────────│                    │                    │
     │                    │                    │                    │
     │ 刷新新闻列表       │                    │                    │
     │                    │                    │                    │
```

---

## 📁 目录结构

```
AINews/
├── src/
│   ├── __init__.py
│   ├── main.py              # 应用入口
│   │
│   ├── api/                 # API 层
│   │   ├── __init__.py
│   │   └── routes/
│   │       ├── news.py      # 新闻接口
│   │       ├── stats.py     # 统计接口
│   │       └── admin.py     # 管理接口
│   │
│   ├── database/            # 数据库层
│   │   ├── __init__.py
│   │   └── connection.py    # 连接管理
│   │
│   ├── models/              # 数据模型
│   │   ├── __init__.py
│   │   └── news.py          # News/Source/Category
│   │
│   ├── scrapers/            # 爬虫模块
│   │   ├── __init__.py
│   │   ├── base.py          # 基类
│   │   ├── rss_scraper.py   # RSS 爬虫
│   │   └── web_scraper.py   # 网页爬虫
│   │
│   ├── processors/          # 处理模块
│   │   ├── __init__.py
│   │   ├── deduplicator.py  # 去重
│   │   ├── classifier.py    # 分类
│   │   ├── summarizer.py    # 摘要
│   │   └── scorer.py        # 评分
│   │
│   ├── scheduler/           # 调度模块
│   │   ├── __init__.py
│   │   └── tasks.py         # 定时任务
│   │
│   └── utils/               # 工具
│       ├── __init__.py
│       ├── logger.py        # 日志
│       └── helpers.py       # 辅助函数
│
├── static/
│   └── index.html           # 前端页面
│
├── config.yaml              # 配置文件
├── requirements.txt         # 依赖
└── ainews.db               # SQLite 数据库
```

---

## 🔑 关键技术点

### 1. 异步处理
- FastAPI 原生支持 async/await
- 后台任务使用 `BackgroundTasks`
- 前端使用轮询检查状态

### 2. 数据去重
- URL 唯一索引（数据库级别）
- 标题相似度检测（业务级别）

### 3. 超时处理
- RSS 请求 10 秒超时
- 前端轮询最多 2 分钟

### 4. 错误处理
- 数据库事务回滚
- 爬虫异常捕获继续
- 前端错误通知

### 5. 配置化
- `config.yaml` 集中管理
- 数据源、关键词、调度时间可配置

